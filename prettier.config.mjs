const config = {
    plugins: ['@trivago/prettier-plugin-sort-imports'],
    singleQuote: true,
    semi: false,

    trailingComma: 'all',

    tabWidth: 4,

    printWidth: 80,
}

export default config
